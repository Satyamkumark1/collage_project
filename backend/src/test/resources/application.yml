spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studyflow_test
    username: cashify
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
